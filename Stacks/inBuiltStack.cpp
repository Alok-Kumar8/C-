#include <bits/stdc++.h>
using namespace std;

int main(){
    stack<int> p;
    // s.push(10);
    // s.push(20);
    // s.push(30);
    // s.push(40);
    // s.push(50);
    // s.push(60);
    // s.push(70);
    // cout << s.top()<< endl;
    // s.pop();
    // s.pop();
    // cout << s.top() << endl;
    // cout << s.empty() << endl;

    string s = "{a+b[c*d+(a+s)]}";
    int len = s.size();
    for(int i = 0; i<len; i++){

    }
    cout << len << endl; 
}